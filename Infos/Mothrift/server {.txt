server {
    server_name dev.evibechecks.com;

    location / {
        root        /var/www/admin_evibechecks/build/;
        index       index.html;
        try_files   $uri $uri/ /index.html;
    }

    location /socket.io/ {
        proxy_http_version 1.1;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_pass "http://localhost:3019/socket.io/";
    }

    location /api {
        proxy_pass "http://localhost:3019";
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_cache_bypass $http_upgrade;
    }

    location /upload {
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_pass http://localhost:3019/upload;
    }
}

server {
    server_name www.dev.evibechecks.com;

    location / {
        root        /var/www/admin_evibechecks/build/;
        index       index.html;
        try_files   $uri $uri/ /index.html;
    }

    location /socket.io/ {
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_pass http://localhost:3019/socket.io/;
    }

    location /api {
        proxy_pass http://localhost:3019;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_cache_bypass $http_upgrade;
    }

    location /upload {
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_pass http://localhost:3019/upload;
    }
}

server {
    server_name payments.dev.evibechecks.com;
    location /api {
        proxy_pass "http://localhost:3019";
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_cache_bypass $http_upgrade;
    }
}
